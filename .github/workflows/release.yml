name: Release

on:
  workflow_dispatch:  # Manual trigger only - no automatic releases

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.11

    - name: Create virtual environment
      run: uv venv

    - name: Install dependencies
      run: |
        uv pip install -e ".[dev]"

    - name: Run tests
      run: |
        uv run python -m pytest

    - name: Build Package
      run: |
        uv build
        echo "Package built successfully in dist/ directory"
        echo "Users can install directly from this private repository"

    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: Release ${{ github.ref_name }}
        draft: false
        prerelease: false
        body: |
          ## 🔒 Private Repository Package
          
          This is a **private repository** for internal use only.
          
          ## 📦 Installation
          
          Install directly from this private GitHub repository:
          
          ```bash
          # Install latest version from main branch
          pip install git+https://github.com/nodetree-io/prisma-shared.git
          
          # Install from specific branch
          pip install git+https://github.com/nodetree-io/prisma-shared.git@main
          
          # For development
          git clone https://github.com/nodetree-io/prisma-shared.git
          cd prisma-shared
          uv pip install -e .
          ```
          
          ## 🔐 Access Requirements
          
          - ✅ GitHub repository access required
          - ✅ Private repository - no public access
          - ❌ Not available on PyPI or any public registry
          
          ## 📋 Recent Changes
          
          - Bug fixes and improvements
          - Updated dependencies
          - Enhanced logging capabilities
          
          ## 📚 Documentation
          
          See [README.md](https://github.com/nodetree-io/prisma-shared#readme) for usage examples.
          
          ## 🚀 Development Setup
          
          ```bash
          git clone https://github.com/nodetree-io/prisma-shared.git
          cd prisma-shared
          uv pip install -e .
          uv run python -m pytest tests/
          ```
